# Leave unset for local dev (uses Vite's proxy to http://localhost:4000).
# For production (Render, etc.), set this to your deployed backend URL, e.g.:
# VITE_API_URL=https://madamsaab-api.onrender.com/api/v1
VITE_API_URL=
